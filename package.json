{
  "name": "angies-sandbox-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static website root. index.html is the Deception Decoder; the showcase sites live under /showcase.",
  "scripts": {
    "build": "cd deception-decoder && node build-standalone.js",
    "test": "cd deception-decoder && node --test tests/*.test.js",
    "dev": "python3 -m http.server 8080"
  }
}
